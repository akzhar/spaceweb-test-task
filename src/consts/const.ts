// application routes
export enum AppRoute {
  HOME = '/',
  NOTIFICATIONS = '/notifications',
  LOGOUT = '/logout',
  ACCOUNT = '/account',
  VPS = '/vps',
  DOMAINS = '/domains',
  ARTICLES = '/'
}

// api routes
export enum ApiRoute {
  GET_ARTICLES = 'https://sweb.ru/export/turbojournal/'
}
